{{outlet}}

{{#bs-dropdown class="sales-order-actions-dropdown-toggle" as |dd| }}
  {{#dd.button class="dropdown-toggle" data-test-sales-order-actions-toggle=true}}
    Menu
  {{/dd.button}}
  {{#dd.menu renderInPlace=false as |menu|}}

    {{#if order.mayBeCancelled}}
      <button
        class="dropdown-item open-modal-button"
        {{action (mut modal1) true}}
        data-test-sales-order-cancel-button
      >
        Open model
      </button>
    {{/if}}

  {{/dd.menu}}
{{/bs-dropdown}}

{{#if modal1}}
  {{#bs-modal-simple open=modal1 title="Simple Dialog" size="sm" onHidden=(action (mut modal1) false)}}
    Hi there
  {{/bs-modal-simple}}
{{/if}}
